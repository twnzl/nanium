{
  "name": "nocat",
  "version": "0.1.29",
  "description": "software architecture framework for no communication & assembling trouble",
  "repository": {},
  "license": "UNLICENSED",
  "private": false,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "keywords": [
    "nanoservices",
    "framework",
    "typescript"
  ],
  "author": "twnzl",
  "scripts": {
    "lint": "tslint \"'src/**/*.ts\" -p tsconfig.json -c tslint.json -e node_modules",
    "build": "rm -r dist && tsc",
    "clean": "rm -r dist",
    "test": "node $NODE_DEBUG_OPTION node_modules/jasmine/bin/jasmine dist/**/*.spec.js",
    "prepare": "npm run build",
    "prepublishOnly": "npm test && npm run lint"
  },
  "files": [
    "dist/**/*"
  ],
  "dependencies": {
    "jasmine-ts": "^0.3.0",
    "recursive-readdir": "^2.2.2",
    "ts-node": "^8.1.0"
  },
  "devDependencies": {
    "@types/jasmine": "^3.3.12",
    "@types/node": "10.14.7",
    "@types/recursive-readdir": "^2.2.0",
    "@types/typescript": "^2.0.0",
    "jasmine": "^3.4.0",
    "jasmine-spec-reporter": "^4.2.1",
    "jasmine-ts-console-reporter": "^3.1.1",
    "typescript": "^3.4.5"
  }
}
